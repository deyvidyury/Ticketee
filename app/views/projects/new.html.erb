<h1>New Project</h1>
<%= form_for(@project) do |f| %>
	<P>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</P>

	<p>
		<%= f.label :description %><br>
		<%= f.text_field :description %>
	</p>

	<%= f.submit %>

<% end %>